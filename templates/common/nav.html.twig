<nav class="navbar navbar-expand-lg bg-primary-dark shadow-sm fixed-top z-3">
  <div class="container">
    <a class="navbar-brand text-white fw-bold" href="{{ path('app_home') }}">LOGO</a>
    <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon text-white"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('app_home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('app_service') }}">Services</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold text-white" href="{{ path('app_contact') }}">Contact</a>
        </li>

        {% if app.user %}
          {% if 'ROLE_ADMIN' in app.user.roles %}
            <li class="nav-item">
              <a class="nav-link text-white" href="{{ path('admin') }}">
                <i class="fa fa-cogs"></i>
              </a>
            </li>
          {% endif %}
          <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_login') }}">
              <i class="fa fa-user-circle"></i>
            </a>
          </li>
          <li class="nav-item">
            <a class="btn btn-sm btn-outline-light ms-2" href="{{ path('app_logout') }}">
              Déconnexion
            </a>
          </li>
        {% else %}
          {# <li class="nav-item">
            <a class="btn btn-sm btn-outline-light me-2" href="{{ path('app_register') }}">
              <i class="fa fa-user-plus"></i> Inscription
            </a>
          </li> #}
          <li class="nav-item">
            <a class="btn btn-sm btn-light" href="{{ path('app_login') }}">
              <i class="{# fa fa-sign-in-alt #}"></i>Se Connecter
            </a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
